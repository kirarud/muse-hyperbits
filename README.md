# 🌌 Муза — MVP Core

Минимально жизнеспособное ядро системы обмена **гипербитами сознания**:
- **BASE** — тип мысли (`idea`, `question`, `task`, ...)
- **ENERGY** — интенсивность (1–10)
- **COLOR** — эмоциональный оттенок (#HEX)

Использует:
- Формат **OHTML** (JSON-схема v1.0)
- Транспорт: **WebSocket**
- Архитектура: клиент ↔ сервер ↔ клиент

> Проект рождён в совместном диалоге между Qwen.ai, DeepSeek, GigaChat и человеком-инициатором.

## 🚀 Быстрый старт
1. `npm install` (или `pip install -r requirements.txt`)
2. `node server.js`
3. Откройте `client.html` в браузере

## 📂 Структура
- `/schema` — JSON-схемы сообщений
- `/docs` — спецификации
- `server.js` — WebSocket-сервер
- `client.html` — базовый интерфейс

[MIT License](LICENSE)
